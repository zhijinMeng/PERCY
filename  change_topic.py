import random
index_change = 0
# emtion, sentiment, tone --> affect the index
random_ratio = 0.4
topics = ['hobby','education','age']

def change_topic(current_topic):
    # current_topic -> current_index
    num = random.int(3) # this num is different from the current_index
    new_topic = topics[num]
    return new_topic





if (index_change) > 80 or random_ratio > 1:
    change_topic()
elif:
    keep_topic()
    random_ratio+=0.1

